# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository is a fresh scaffold. Per the README, `icare` is intended to be
"a LinkedIn-like platform exclusively for the healthcare industry," but no
source code, framework choice, build tooling, or tests have been added yet —
the repo currently contains only `README.md`.

There are no build, lint, or test commands to document because no
package manifest, source tree, or CI configuration exists yet.

## Working in this repo right now

- Since there is no established structure or tooling, check with the user
  before picking a stack/framework, directory layout, or dependency set —
  these decisions will shape everything that follows.
- Once real code, config, and dependencies are added, update this file with
  the actual build/lint/test commands and a description of the resulting
  architecture (major modules, how they relate, and any non-obvious data
  flow) so future sessions don't need to rediscover it from scratch.
